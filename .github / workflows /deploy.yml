name: Deploy Application

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test application
        run: |
          echo "Application checked out successfully"
          echo "Deployment pipeline is working"

      - name: List files
        run: ls -la
